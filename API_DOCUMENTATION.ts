/**\n * COMPREHENSIVE JSDOC DOCUMENTATION\n * For drunk-pulumi-cloudflare-components Project\n * \n * This file provides complete JSDoc documentation for all classes and interfaces.\n * Copy and paste the relevant documentation blocks above each class definition.\n */\n\n// ============================================================================\n// BASE CLASSES DOCUMENTATION\n// ============================================================================\n\n/**\n * Abstract base component class for Pulumi resource components.\n * \n * This class serves as a foundation for creating custom Cloudflare infrastructure components\n * within the Pulumi framework. It extends Pulumi's ComponentResource and provides:\n * - Automatic type namespace management for resource types\n * - Cloudflare account ID and zone ID management from environment variables\n * - Standard output registration pattern\n * - Type-safe generic arguments handling\n *\n * @template TArgs - Generic type parameter extending pulumi.Inputs\n * @extends {pulumi.ComponentResource<TArgs>}\n * @abstract\n */\nexport abstract class BaseComponent<TArgs extends pulumi.Inputs> extends pulumi.ComponentResource<TArgs> {\n  protected readonly accountId?: pulumi.Output<string>;\n  protected readonly zoneId?: pulumi.Output<string>;\n  protected constructor(\n    type: string,\n    public readonly name: string,\n    protected readonly args: TArgs,\n    protected readonly opts?: pulumi.ComponentResourceOptions,\n  );\n  public abstract getOutputs(): pulumi.Inputs | pulumi.Output<pulumi.Inputs>;\n  protected registerOutputs(): void;\n}\n\n/**\n * Abstract base provider class for Pulumi dynamic resources.\n * \n * Implements the pulumi.dynamic.ResourceProvider interface and provides a foundation\n * for creating custom dynamic resource providers. Dynamic providers allow creating\n * resources with custom lifecycle management (create, read, update, delete, diff, check).\n *\n * @template TInputs - The type definition for resource inputs\n * @template TOutputs - The type definition for resource outputs\n * @implements {pulumi.dynamic.ResourceProvider}\n * @abstract\n */\nexport abstract class BaseProvider<TInputs, TOutputs> implements pulumi.dynamic.ResourceProvider {\n  public abstract create(inputs: TInputs): Promise<pulumi.dynamic.CreateResult<TOutputs>>;\n}\n\n/**\n * Cloudflare domain client for certificate and DNS management.\n * \n * Provides methods to interact with Cloudflare's API for domain-related operations:\n * - Universal SSL certificate management\n * - Mutual TLS (mTLS) client certificate management\n * - Origin CA certificate management\n * - DNS record operations\n * \n * @class\n * @throws {Error} If CLOUDFLARE_API_TOKEN environment variable is not set\n */\nexport class CfDomainClient {\n  private _client: Cloudflare;\n  private _zone: Cloudflare.Zones.Zone | undefined;\n  constructor(private domain: string);\n  public async getZone(): Promise<Cloudflare.Zones.Zone>;\n  public async getUniversalCerts(): Promise<EdgeCertType[]>;\n  public async ensureUniversalCertEnabled(): Promise<void>;\n  public async getMtlsClientCerts(): Promise<OriginCertType[]>;\n  public async createMtlsClientCert(csr: string): Promise<any>;\n  public async getOriginCerts(): Promise<OriginCertType[]>;\n  public async createOriginCerts(csr: string): Promise<any>;\n}\n\n// ============================================================================\n// MAIN ACCOUNT COMPONENT DOCUMENTATION\n// ============================================================================\n\n/**\n * Main Cloudflare Zero Trust Account component.\n * \n * This is the primary orchestration component for managing Cloudflare Zero Trust infrastructure.\n * It manages and coordinates the creation of:\n * \n * **Core Infrastructure:**\n * - Zero Trust organization with auth domain\n * - Azure AD identity provider integration for SSO via Entra ID\n * - Device management, enrollment policies, and compliance rules\n * - Device posture rules (WARP, Gateway, Intune compliance)\n * - Cloudflare tunnels with virtual networks for private applications\n * - Gateway certificates and security settings\n * \n * **Key Features:**\n * - Automated Azure Identity creation and SCIM provisioning\n * - Multi-device platform support (Windows, Mac, Linux, iOS, Android, ChromeOS)\n * - Intune device compliance integration\n * - Automatic tunnel secret generation and vault storage\n * - Device exclusion list management with CIDR tools\n * - Policy-based application access control\n * \n * @class\n * @extends {BaseComponent<CloudflareZeroTrustAccountArgs>}\n * @example\n * ```typescript\n * const account = new CloudflareZeroTrustAccount('my-org', {\n *   organization: { name: 'myorg' },\n *   enableAzIdentity: true,\n *   enableDeviceIntuneIntegration: true,\n *   tunnels: [\n *     {\n *       name: 'prod-tunnel',\n *       applications: [\n *         {\n *           name: 'backend-api',\n *           privateIpAddresses: [{ cidr: '10.0.0.0/24', port: 8080 }],\n *           policies: { requiredSso: true, requiredDevicePosture: true }\n *         }\n *       ]\n *     }\n *   ]\n * });\n * ```\n */\nexport class CloudflareZeroTrustAccount extends BaseComponent<CloudflareZeroTrustAccountArgs> {\n  public readonly organization?: cf.ZeroTrustOrganization;\n  public readonly identityProvider?: cf.ZeroTrustAccessIdentityProvider;\n  public readonly onboardingGroup?: AzRole;\n  public readonly devicesPostureRules?: DevicesPostureRulesType;\n  public readonly ssoPolicy?: cf.ZeroTrustAccessPolicy;\n  public readonly deviceEnrollmentPolicy?: cf.ZeroTrustAccessPolicy;\n  public readonly devicePosturePolicy?: cf.ZeroTrustAccessPolicy;\n  public readonly defaultTunnelNetwork?: cf.ZeroTrustTunnelCloudflaredVirtualNetwork;\n  public readonly tunnels?: Record<string, TunnelOutputType>;\n}\n\n// ============================================================================\n// ZERO TRUST APPLICATION COMPONENTS DOCUMENTATION\n// ============================================================================\n\n/**\n * Zero Trust Access Application component for private and public applications.\n * \n * Manages Cloudflare Access applications that can serve both:\n * - **Public routes**: Accessible from the internet with Cloudflare Authentication\n * - **Private routes**: Accessed through Cloudflare Tunnel with device compliance\n * \n * Supports routing to:\n * - Public hostnames (CNAME routing via tunnel)\n * - Private hostnames (internal backends via tunnel)\n * - Private IP addresses (CIDR ranges via tunnel)\n * \n * Automatically creates DNS records for public routes and applies access policies.\n * \n * @class\n * @extends {BaseComponent<ZeroTrustApplicationArgs>}\n */\nexport class ZeroTrustApplication extends BaseComponent<ZeroTrustApplicationArgs> {\n  public readonly appId: pulumi.Output<string>;\n}\n\n/**\n * Zero Trust Application for public, unauthenticated routes.\n * \n * Manages routes and DNS records for applications that allow public internet access\n * without requiring Cloudflare authentication. Uses Cloudflare Tunnels for backend routing.\n * \n * Automatically creates:\n * - DNS CNAME records pointing to the tunnel\n * - Tunnel ingress routes for HTTP/HTTPS/TCP/UDP\n * - 404 fallback responses\n * \n * @class\n * @extends {BaseComponent<ZeroAnonymousApplicationArgs>}\n */\nexport class ZeroAnonymousApplication extends BaseComponent<ZeroAnonymousApplicationArgs> {\n  constructor(name: string, args: ZeroAnonymousApplicationArgs, opts?: pulumi.ComponentResourceOptions);\n}\n\n// ============================================================================\n// ZERO TRUST SETTINGS RESOURCES DOCUMENTATION\n// ============================================================================\n\n/**\n * Dynamic provider for configuring Zero Trust connectivity settings.\n * \n * Manages ICMP proxy and WARP offramp settings for Zero Trust networks.\n * Controls low-level connectivity options for devices connected via WARP.\n * \n * @class\n * @extends {BaseProvider<ZeroTrustConnectivitySettingsInputs, ZeroTrustConnectivitySettingsOutputs>}\n */\nexport class ZeroTrustConnectivitySettingsProvider extends BaseProvider<ZeroTrustConnectivitySettingsInputs, ZeroTrustConnectivitySettingsOutputs> {\n  public async create(inputs: ZeroTrustConnectivitySettingsInputs): Promise<pulumi.dynamic.CreateResult>;\n}\n\n/**\n * Dynamic resource for Zero Trust connectivity settings.\n * \n * Configures network-level connectivity options for Zero Trust deployments.\n * \n * @class\n * @extends {BaseResource<ZeroTrustConnectivitySettingsInputs, ZeroTrustConnectivitySettingsOutputs>}\n */\nexport class ZeroTrustConnectivitySettingsResource extends BaseResource<ZeroTrustConnectivitySettingsInputs, ZeroTrustConnectivitySettingsOutputs> {\n}\n\n/**\n * Dynamic provider for configuring device-level settings in Zero Trust.\n * \n * Manages WARP client configuration including:\n * - Override code bypass duration for temporary access\n * - Gateway proxy filtering for TCP and UDP traffic\n * - Root certificate installation policies\n * - Virtual IPv4 (CGNAT) assignment options\n * \n * These settings apply to all enrolled devices in the account.\n * \n * @class\n * @extends {BaseProvider<ZeroTrustDeviceSettingsInputs, ZeroTrustDeviceSettingsOutputs>}\n */\nexport class ZeroTrustDeviceSettingsProvider extends BaseProvider<ZeroTrustDeviceSettingsInputs, ZeroTrustDeviceSettingsOutputs> {\n  public async create(inputs: ZeroTrustDeviceSettingsInputs): Promise<pulumi.dynamic.CreateResult>;\n}\n\n/**\n * Dynamic resource for Zero Trust device settings.\n * \n * Applies device-level configuration to all WARP-enrolled devices.\n * \n * @class\n * @extends {BaseResource<ZeroTrustDeviceSettingsInputs, ZeroTrustDeviceSettingsOutputs>}\n */\nexport class ZeroTrustDeviceSettingsResource extends BaseResource<ZeroTrustDeviceSettingsInputs, ZeroTrustDeviceSettingsOutputs> {\n}\n\n/**\n * Dynamic provider for configuring Zero Trust Access WARP policies.\n * \n * Manages WARP-specific access policies and maps identity providers\n * to WARP enrollment and authentication flows.\n * \n * @class\n * @extends {BaseProvider<ZeroTrustAccessWarpInputs, ZeroTrustAccessWarpOutputs>}\n */\nexport class ZeroTrustAccessWarpProvider extends BaseProvider<ZeroTrustAccessWarpInputs, ZeroTrustAccessWarpOutputs> {\n  public async create(inputs: ZeroTrustAccessWarpInputs): Promise<pulumi.dynamic.CreateResult>;\n}\n\n/**\n * Dynamic resource for Zero Trust WARP access configuration.\n * \n * Configures which identity providers are available for WARP enrollment.\n * \n * @class\n * @extends {BaseResource<ZeroTrustAccessWarpInputs, ZeroTrustAccessWarpOutputs>}\n */\nexport class ZeroTrustAccessWarpResource extends BaseResource<ZeroTrustAccessWarpInputs, ZeroTrustAccessWarpOutputs> {\n}\n\n/**\n * Dynamic provider for activating Cloudflare Gateway certificates.\n * \n * Manages the lifecycle of gateway certificates, handling both\n * activation and status tracking throughout the certificate lifecycle.\n * \n * @class\n * @extends {BaseProvider<ZeroTrustGatewayCertificateActivationInputs, ZeroTrustGatewayCertificateActivationOutputs>}\n */\nexport class ZeroTrustGatewayCertificateActivationProvider extends BaseProvider<ZeroTrustGatewayCertificateActivationInputs, ZeroTrustGatewayCertificateActivationOutputs> {\n  public async create(inputs: ZeroTrustGatewayCertificateActivationInputs): Promise<pulumi.dynamic.CreateResult>;\n}\n\n/**\n * Dynamic resource for Gateway certificate activation.\n * \n * Ensures gateway certificates are properly activated and tracks their status.\n * \n * @class\n * @extends {BaseResource<ZeroTrustGatewayCertificateActivationInputs, ZeroTrustGatewayCertificateActivationOutputs>}\n */\nexport class ZeroTrustGatewayCertificateActivationResource extends BaseResource<ZeroTrustGatewayCertificateActivationInputs, ZeroTrustGatewayCertificateActivationOutputs> {\n}\n\n// ============================================================================\n// DOMAIN MANAGEMENT COMPONENTS DOCUMENTATION\n// ============================================================================\n\n/**\n * Dynamic provider for managing DNS records in Cloudflare.\n * \n * Handles complete CRUD operations for DNS records with automatic upsert functionality.\n * Features:\n * - Automatic creation or update of existing records\n * - Intelligent record matching by subdomain and type\n * - Automatic cleanup of records no longer in configuration\n * - Error recovery and duplicate handling\n * - Support for all major DNS record types (A, AAAA, CNAME, TXT, MX, NS, SRV, CAA)\n * \n * @class\n * @extends {BaseProvider<DnsRecordsInputs, DnsRecordsOutputs>}\n */\nexport class DnsRecordsProvider extends BaseProvider<DnsRecordsInputs, DnsRecordsOutputs> {\n  public async create(inputs: DnsRecordsInputs): Promise<pulumi.dynamic.CreateResult>;\n  public async update(id: string, olds: DnsRecordsOutputs, news: DnsRecordsInputs): Promise<pulumi.dynamic.UpdateResult>;\n  public async delete(id: string, props: DnsRecordsOutputs): Promise<void>;\n}\n\n/**\n * Dynamic resource for managing DNS records.\n * \n * Wraps the DnsRecordsProvider to provide a Pulumi resource interface\n * for declarative DNS record management across Cloudflare zones.\n * \n * @class\n * @extends {BaseResource<DnsRecordsInputs, DnsRecordsOutputs>}\n */\nexport class DnsRecordsResource extends BaseResource<DnsRecordsInputs, DnsRecordsOutputs> {\n  declare readonly name: string;\n  public readonly recordIds!: pulumi.Output<string[]>;\n}\n\n/**\n * Dynamic provider for managing Cloudflare Origin CA certificates.\n * \n * Retrieves existing origin certificates from Cloudflare or creates new ones.\n * Automatically checks certificate availability and handles certificate lifecycle.\n * \n * @class\n * @extends {BaseProvider<OriginCertInputs, OriginCertOutputs>}\n */\nexport class OriginCertProvider extends BaseProvider<OriginCertInputs, OriginCertOutputs> {\n  public async create(inputs: OriginCertInputs): Promise<pulumi.dynamic.CreateResult>;\n}\n\n/**\n * Dynamic resource for managing Cloudflare Origin CA certificates.\n * \n * Wraps the OriginCertProvider to provide a Pulumi resource interface\n * for managing origin certificates used for mTLS between Cloudflare and origin servers.\n * \n * @class\n * @extends {BaseResource<OriginCertInputs, OriginCertOutputs>}\n */\nexport class OriginCertResource extends BaseResource<OriginCertInputs, OriginCertOutputs> {\n  declare readonly name: string;\n  declare readonly certificate: pulumi.Output<string | undefined>;\n  declare readonly expires_on: pulumi.Output<string | undefined>;\n  declare readonly hostnames: pulumi.Output<string[]>;\n}\n